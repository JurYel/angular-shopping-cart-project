export interface CartItem {
    id: string
    username: string
    item_img: string[]
    item_name: string[]
    category: string[]
    quantity: number[]
    unit_price: number[]
    subtotal: number[]
}
